# Render deploy hook – trigger deploy without logging in.
# Get it: Render Dashboard → webpoint-dep-highlighter → Settings → Deploy Hook → copy URL.
# Then: cp .env.example .env && edit .env and paste your URL.
RENDER_DEPLOY_HOOK_URL=https://api.render.com/deploy/srv-XXXXXXXX?key=XXXX
