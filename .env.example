# Render deploy: use either deploy hook (preferred) or API key + service ID
# Deploy hook: Render Dashboard → webpoint-dep-highlighter → Settings → Deploy Hook → copy URL
# RENDER_DEPLOY_HOOK_URL=https://api.render.com/deploy/srv-xxx?key=xxx

# Or use API trigger (TRIGGER_DEPLOY.sh will use this if RENDER_DEPLOY_HOOK_URL is not set)
# RENDER_API_KEY=your_render_api_key
# RENDER_SERVICE_ID=srv-d616lm2li9vc73fqqps0
